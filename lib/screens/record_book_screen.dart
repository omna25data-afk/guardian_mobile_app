// Placeholder for the Record Book screen
import 'package:flutter/material.dart';

class RecordBookScreen extends StatelessWidget {
  // FIX: Added key to constructor
  const RecordBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // FIX: Added const
    return const Center(
      child: Text('شاشة سجلاتي (قيد الإنشاء)'),
    );
  }
}
